# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

HTML + Tailwind (CDN)، فارسی و RTL. صفحهٔ واقعی در فایل جدیدی کنار دک وایرفریم ساخته می‌شود؛ فایل‌های `*.html` وایرفریم و `assets/wireframe.css` / `assets/wireframe.js` دست‌نخورده می‌مانند.

## Users

بانوانی که به‌دنبال خدمات زیبایی در سالن هستند و می‌خواهند بدون تماس تلفنی نوبت بگیرند؛ خریداران محصولات مراقبت پوست و مو؛ و هنرجویانی که می‌خواهند حرفهٔ آرایشگری را از پایه تا پیشرفته یاد بگیرند. هر سه گروه از یک صفحهٔ اصلی مشترک وارد می‌شوند و ورودی غالب موبایل است.

## Product Purpose

سالن زیبایی المیرا آزمون سه خط کسب‌وکار را زیر یک برند جمع می‌کند: خدمات سالن، فروشگاه محصولات، و آموزشگاه. صفحهٔ اصلی نقش «درواز‌ه» را دارد: در چند ثانیه باید مشخص شود سالن چه کاری می‌کند و کاربر بتواند مستقیم وارد یکی از سه بخش شود. موفقیت یعنی کاربر بدون سردرگمی مسیر خودش را انتخاب کند.

## Positioning

سه خط کسب‌وکار زیر یک نام و یک تجربهٔ واحد — سالن، فروشگاه، آموزشگاه — به‌جای سه سایت جدا. کاربر می‌تواند از مشتری خدمات به خریدار محصول و از آنجا به هنرجو تبدیل شود.

## Operating Context

- ورود عمدتاً از موبایل و از طریق شبکه‌های اجتماعی.
- مسیرهای اصلی سایت (موجود در وایرفریم): خدمات → جزئیات خدمت → لینک سامانهٔ نوبت‌دهی سالن؛ فروشگاه → جزئیات محصول → سبد → تسویه؛ آموزشگاه → جزئیات دوره → ثبت‌نام؛ به‌علاوهٔ گالری، مجله، دربارهٔ ما، تماس، ورود/ثبت‌نام و پنل کاربری.
- نوبت‌دهی در سیستم داخلی سالن انجام می‌شود، نه در سایت. سایت فقط لینک آن سیستم را قرار می‌دهد (در تب جدید)؛ فرم رزرو، تقویم، انتخاب ساعت، تأیید نوبت و مدیریت نوبت در پنل کاربری در سایت وجود ندارد. آدرس لینک در `BOOKING_URL` داخل `assets/wireframe.js` تعریف شده و هر عنصر با `data-booking` به آن وصل می‌شود.

## Capabilities and Constraints

- فارسی، RTL، فونت Vazirmatn.
- قیمت‌ها به تومان و اعداد فارسی.
- سه ورودی اصلی صفحه اصلی هم‌وزن‌اند؛ هیچ‌کدام از سه بخش نباید دو بخش دیگر را پنهان کند.
- تاریخ‌ها شمسی‌اند.
- بک‌اند هنوز تعیین نشده؛ صفحه اصلی فعلاً استاتیک است و فرم‌ها به صفحات موجود لینک می‌شوند.

## Brand Commitments

نام «المیرا آزمون». لوگو هنوز وجود ندارد. رنگ صورتیِ خاکی `#b3536b` و مشکیِ گرم `#232120` در وایرفریم به‌کار رفته‌اند اما به‌عنوان تعهد برند تأیید نشده‌اند — شواهدی از ترجیح رنگی، نه قید الزام‌آور.

## Evidence on Hand

هیچ محتوا، عکس، لوگو، آمار یا نظر مشتری واقعی در دست نیست. تمام متن‌ها، قیمت‌ها، نام خدمات، آمار (سال تجربه، تعداد مشتری) و نظرات در صفحهٔ ساخته‌شده **نمونه** هستند و باید پیش از انتشار با محتوای واقعی جایگزین شوند. هیچ ادعای واقعی (گواهی‌نامه، مجوز، برند همکار) نباید ساخته شود.

## Product Principles

1. سه‌راهی روشن: کاربر در نخستین برخورد باید بفهمد سه مسیر وجود دارد و کدام مال اوست.
2. لینک سامانهٔ نوبت‌دهی باید کوتاه‌ترین فاصله را از هر نقطهٔ صفحه داشته باشد، بدون اینکه دو بخش دیگر را له کند.
3. موبایل مبنای طراحی است، نه حالت جانبی.
4. اعتماد پیش از فروش: کار واقعی سالن (نمونه‌کار، فضا، افراد) باید دیده شود.
5. هیچ ادعای اثبات‌نشده‌ای ساخته نمی‌شود؛ جای محتوای واقعی به‌روشنی علامت‌گذاری می‌شود.

## Accessibility & Inclusion

RTL کامل، کنتراست کافی برای متن روی تصویر، اندازهٔ هدف لمسی مناسب موبایل، و احترام به `prefers-reduced-motion`.
