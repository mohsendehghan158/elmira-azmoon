# Design

دنیای بصری صفحهٔ اصلی المیرا آزمون، نوشته‌شده از روی چیزی که واقعاً ساخته شد
(`home.html` + `assets/home.css` + `assets/home.js`)، نه از روی نیت اولیه.

> دک وایرفریم (`index.html` و بقیهٔ `*.html` به‌همراه `assets/wireframe.css`) دنیای دیگری است
> و این سند بر آن حاکم نیست. آن‌ها دست‌نخورده باقی مانده‌اند.

## دنیا: سردرِ مینایی

تابلوی دست‌نویسِ مینا روی فلز؛ همان چیزی که بالای سردر مغازه‌های ایرانی آویزان است.
منطق کل صفحه از همین می‌آید: هر بلوکِ محتوا یک «تابلو» است، سه خط کسب‌وکار سه تابلوی
آویزان از یک ریل‌اند، و فرم رزرو همان نوارِ شمارهٔ تلفن پای تابلوست.

قرعهٔ impeccable: `--scope direction --mode persuade`، seed `ba1172db`، جایگاه ۷ از فهرست
جهت‌گیری‌های زمینی. قرارداد جهت‌گیری به‌صورت کامنت HTML، اولین فرزند `<body>` در
`home.html` ثبت شده است.

**حالت (mode):** Persuade. بازدیدکننده باید در چند ثانیه بفهمد سه مسیر وجود دارد و
یکی را بردارد.

## رنگ

استراتژی: **Drenched** — صفحه خودش ورقِ مینا است، نه زمینهٔ خنثی با چند لهجه.
دو ورق، یک دنیا: تیره پیش‌فرض است (صحنهٔ استفاده: شب، موبایل در دست)، روشن همان
تابلو زیر نور روز است. هر دو در `:root` و `:root[data-theme="light"]` تعریف شده‌اند و
`prefers-color-scheme` هم پوشش دارد.

| نقش | توکن | تیره | روشن |
|---|---|---|---|
| زمینه | `--ground` | `#0e3a31` | `#dfe6e0` |
| زمینهٔ عمیق (نوارها، فوتر) | `--ground-deep` | `#0a2b25` | `#cfd9d1` |
| رویهٔ تابلو | `--plaque` | `#103f35` | `#f4f6f2` |
| حروف | `--ink` | `#f2f5ee` | `#10231d` |
| متن فرعی | `--ink-2` | `#b9cbc2` | `#4d635a` |
| لهجه (تنها لهجه) | `--hot` | `#e0451f` | `#cd3a15` |
| طلاییِ حروف و خط‌کشی | `--gold` | `#efb833` | `#a97a10` |

قواعدی که باید نگه داشته شوند:

- **یک لهجه.** `--hot` تنها رنگِ کنش است: دکمهٔ اصلی، خط‌کشیِ بیرونی، لینک‌های «دیدن…»،
  ستاره‌ها، حالت focus. هیچ رنگ دوم برای CTA وارد صفحه نمی‌شود.
- **طلایی جنس است، نه دکمه.** `--gold` فقط برای حروفِ تابلو، قیمت‌ها و خط‌کشیِ داخلی.
  هرگز پس‌زمینهٔ دکمه نمی‌شود.
- کِرِم و بژ عمداً بیرون است. زمینهٔ روشن، مینای کم‌رنگِ سبز-خاکستری است نه کاغذ.

## حروف

- **نمایشی:** `Lalezar` (Google Fonts) با کلاس `.sign`. انتخاب از روی همین دنیاست؛
  لاله‌زار از روی حروفِ تابلوهای دست‌نویس ایرانی طراحی شده.
- **متن و رابط:** `Vazirmatn` وزن‌های ۴۰۰ تا ۷۰۰.
- **سایه‌زنیِ حروف** (`.painted`) گرامرِ خودِ تابلوست: یک سایهٔ سختِ ۲ پیکسلی به‌علاوهٔ
  یک محوِ نرم. در ورقِ روشن به هایلایت سفید + سایهٔ کم‌عمق تبدیل می‌شود.
- مقیاس تیترها با `clamp()` سیال است؛ سقفِ نمایشی ۵rem.
- طول سطرِ متن `46ch` تا `52ch`. `line-height` بدنه `1.85` چون فارسی کشیدگی عمودی دارد.
- اعداد فارسی‌اند و روی `.num` با `tabular-nums` تراز می‌شوند.

## شکل و جنس

- **یک شعاع برای همه‌چیز:** `--r: 4px`. تنها استثناها دایره‌ای‌اند و دلیل فیزیکی دارند:
  پیچ‌های گوشه، `.emblem` و `.monogram` و `.social`.
- **`.plaque`** واحدِ پایه است. رویه + سایهٔ فاصله‌دار و محو (بدون هالهٔ بی‌آفست).
- **خط‌کشیِ دوتایی** (`.plaque::before`) امضای دنیاست: حلقهٔ شنگرف بیرون، خطِ طلاییِ
  یک‌پیکسلی درون، در `inset: 8px`. کلاس `.no-rule` آن را برای تابلوهای کوچک برمی‌دارد
  تا صفحه شلوغ نشود.
- **`.riveted`** چهار پیچ گوشه را با چهار `radial-gradient` می‌گذارد.
- **`.hooks` + `.rail-line`** سه تابلوی اصلی را از یک ریل آویزان می‌کند.
- **دانهٔ رنگ** روی `body::after` است: `position: fixed`، `pointer-events: none`،
  `mix-blend-mode: overlay`. هرگز روی کانتینرِ اسکرول‌شونده نرود.

## چیدمان

`max-w-[1400px]` با `px-5 lg:px-10`. هیچ دو بخشی خانوادهٔ چیدمانی مشترک ندارند:

| بخش | خانواده |
|---|---|
| سردر | تقسیمِ نامتقارن، حروف/عکس، با نوارِ فرم چسبیده به لبهٔ پایین |
| سه کار | سه تابلوی آویزانِ نابرابر از یک ریل |
| خدمات | ریلِ افقی با `scroll-snap` |
| آمار | نوارِ تمام‌عرضِ شنگرف |
| نمونه‌کار | گرید با سلول‌های چنداندازه |
| آموزشگاه | عکسِ تمام‌قد با پردهٔ خوانایی و متنِ رویش |
| فروشگاه | بنتوی ۴ سلول برای دقیقاً ۴ محصول |
| نظرات | دو ستونِ پلکانی + یک تابلوی مرکزی |
| مجله | ردیف‌های تصویر + عنوان با خطِ جداکننده |
| بستن | تابلوی مرکزیِ تک‌پیام |

آنچه صفحه عمداً ندارد: **eyebrow** (هیچ برچسبِ ریزِ بالای تیتر)، شمارهٔ بخش،
سه کارتِ هم‌اندازهٔ آیکن+تیتر+متن، متنِ گرادیانی، شیشه‌ٔ تزئینی.

## حرکت

یک لحظهٔ نویسنده‌دار، نه افکت‌های پراکنده: **برقِ مینا**. وقتی تابلو وارد دید می‌شود،
یک باندِ نورِ اریب یک‌بار از رویش می‌گذرد (`.sheen`) و خودِ تابلو ۱۴ پیکسل بالا می‌آید.
شروع از حالتِ دیده‌شده است؛ اگر جاوااسکریپت اجرا نشود محتوا کامل دیده می‌شود.

- محرک: `IntersectionObserver` با `unobserve` پس از اولین بار. هیچ `scroll` listener.
- کاسکِیدِ تأخیر با `data-delay` روی خودِ عنصر.
- حرکتِ ثانویه، از همان دنیا: `.swing` تابلو را ۰٫۵ درجه روی قلاب می‌چرخاند.
- easing واحد: `cubic-bezier(.16, 1, .3, 1)`.
- `prefers-reduced-motion: reduce` همهٔ اینها را کاملاً خاموش می‌کند.

## دسترس‌پذیری

- لینکِ پرش به محتوا، `:focus-visible` با حلقهٔ ۳ پیکسلی شنگرف.
- برچسبِ فرم **بالای** ورودی (`.field-label`)؛ هیچ placeholder به‌جای برچسب.
- منوی موبایل `role="dialog"` با `aria-modal`، بستن با `Escape`، بازگشت فوکوس.
- آیکن‌ها همه `aria-hidden`؛ دکمه‌های بدون متن `aria-label` دارند.
- به‌جای آواتارِ ساختگیِ مشتری، حرفِ اولِ رنگ‌شده (`.monogram`) استفاده می‌شود.

## وابستگی‌ها

| چه | از کجا |
|---|---|
| Tailwind v4 | `cdn.jsdelivr.net/npm/@tailwindcss/browser@4` (کامپایل در مرورگر) |
| Lalezar + Vazirmatn | Google Fonts |
| آیکن‌ها | Phosphor Icons وب، فقط وزن `regular` |
| تصاویر | Unsplash، همه با `loading="lazy"` جز عکس سردر |

آیکن‌ها همه از یک خانواده و یک وزن‌اند. هیچ SVG آیکنِ دست‌ساز در صفحه نیست.

## بدهی‌های شناخته‌شده

- Tailwind در مرورگر کامپایل می‌شود؛ برای انتشار واقعی باید به CSS بیلدشده تبدیل شود
  (LCP و CLS از این بابت بهبود پیدا می‌کنند).
- بازبینی بصری (اسکرین‌شات دسکتاپ و موبایل) در این نشست انجام نشد. هیچ ادعایی دربارهٔ
  درستی چیدمان در مرورگر واقعی ثبت نشده است.
- محتوای نمونه در صفحه علامت‌گذاری شده است؛ فهرست جایگزینی در `PRODUCT.md` بخش
  «Evidence on Hand» توضیح داده شده.
